---
description: Display registered agents with memory counts
allowed-tools: Bash
---

Show all registered agents, their roles, memory counts, and creation dates.

Run this command:
```bash
node "$(npm root -g)/exe-ai-employees/dist/bin/exe-team.js"
```
